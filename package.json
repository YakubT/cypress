{
    "scripts": {
        "startServer": "json-server --watch db.json"
    },
    "devDependencies": {
        "@cypress/xpath": "^2.0.3",
        "cypress": "^3.1.15",
        "cypress-file-upload": "^2.0.0",
        "json-server": "^0.17.3"
    }
}
